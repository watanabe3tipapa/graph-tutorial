name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Lint
        run: npm run lint
      - name: Test
        run: npm test
      - name: Test (worker security unit tests)
        run: npm run test -w worker
      - name: Typecheck (worker)
        run: npm run typecheck -w worker
      - name: Smoke test (data integrity)
        run: npm run smoke
      - name: Build
        run: npm run build