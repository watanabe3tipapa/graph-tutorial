{
  "name": "graph-tutorial-worker",
  "private": true,
  "version": "0.4.0",
  "license": "GPL-2.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --remote",
    "deploy": "wrangler deploy",
    "kv:namespace": "wrangler kv namespace create SNAPSHOTS",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250601.0",
    "typescript": "~5.6.2",
    "vitest": "^3.2.7",
    "wrangler": "^4.0.0"
  }
}
